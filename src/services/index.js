'use strict';

const welcome = require('./welcome');
const slotGeneration = require('./slot-generation');
const subslots = require('./subslots');

module.exports = {
    welcome,
    slotGeneration,
    subslots
}